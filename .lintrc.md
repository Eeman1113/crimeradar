Lint note: Next.js 16 removed `next lint` (per `node_modules/next/dist/docs/01-app/03-api-reference/05-config/03-eslint.md`). The `lint` script now runs `eslint .` (flat config). ESLint and `eslint-config-next` are NOT installed yet — install them and add `eslint.config.mjs` before `npm run lint` will work.
